/**
 * Vue GitHub buttons options.
 *
 * @export
 * @interface Options
 */
export default interface Options {
	/**
	 * Enable session storage caching?
	 *
	 * @type {boolean}
	 * @memberof Options
	 */
	useCache: boolean;
}
